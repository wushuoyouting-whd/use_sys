# 样式模块化说明

## 📁 文件结构

```
src/styles/
├── index.css              # 样式入口文件（统一导入）
├── variables.css          # CSS 变量定义
├── reset.css              # 样式重置
├── theme.css              # Element Plus 主题定制
├── layout.css             # 布局工具类
└── components/            # 组件样式
    ├── app.css           # App 组件样式
    ├── user-list.css     # 用户列表组件样式
    └── user-form.css     # 用户表单组件样式
```

## 🎨 使用方式

### 1. 全局样式

在 `main.js` 中引入：
```javascript
import './styles/index.css'
```

### 2. 组件样式

在组件中使用 `@import`：
```vue
<style scoped>
@import '../styles/components/user-list.css';
</style>
```

### 3. 使用 CSS 变量

在任何样式中使用预定义的变量：
```css
.my-component {
  color: var(--color-primary);
  padding: var(--spacing-md);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-md);
}
```

## 📋 变量说明

### 颜色变量
- `--color-primary`: 主色调（淡蓝色）
- `--color-text-*`: 文本颜色
- `--color-bg-*`: 背景颜色
- `--color-border-*`: 边框颜色

### 尺寸变量
- `--spacing-*`: 间距（xs, sm, md, lg, xl）
- `--radius-*`: 圆角（sm, md, lg）
- `--font-size-*`: 字体大小

### 阴影变量
- `--shadow-sm`: 小阴影
- `--shadow-md`: 中等阴影
- `--shadow-lg`: 大阴影

## 🔧 扩展

添加新组件样式：
1. 在 `styles/components/` 创建新文件
2. 在组件中使用 `@import` 引入

修改主题：
1. 修改 `variables.css` 中的变量值
2. 所有使用该变量的地方会自动更新

## ✨ 优势

- ✅ **模块化**：样式按功能分离，易于维护
- ✅ **可复用**：CSS 变量统一管理，全局生效
- ✅ **易扩展**：新增组件只需创建对应样式文件
- ✅ **易维护**：修改主题只需修改变量文件


